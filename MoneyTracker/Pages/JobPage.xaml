<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Name="JobPageBind"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MoneyTracker.Pages.JobPage"
             Title="Jobs">

    <Grid ColumnDefinitions=".25*,auto,auto,.25*"
          RowDefinitions="auto,auto,auto,auto,auto,auto">
        <Picker Grid.Column="2" x:Name="JobTypePicker" Title="Select Job Type" ItemsSource="{Binding JobTypesValues}" SelectedItem="{Binding JobType}"/>
        <Picker Grid.Column="2" Grid.Row="1" x:Name="JobLocationPicker" Title="Select Job Type" ItemsSource="{Binding JobLocationValues}" SelectedItem="{Binding JobLocation}"/>
        <Picker Grid.Column="2" Grid.Row="2" x:Name="JobStatusPicker" Title="Select Job Type" ItemsSource="{Binding JobStatusValues}" SelectedItem="{Binding JobStatus}"/>
        <Picker Grid.Column="2" Grid.Row="3" x:Name="JobHoursPicker" Title="Select Job Type" ItemsSource="{Binding JobHoursValues}" SelectedItem="{Binding JobHours}"/>
    </Grid>
</ContentPage>